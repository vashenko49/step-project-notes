import {Card} from "./Card";

export class CheckBoxCard extends Card{
    constructor(title,is_img, arrayTask){
        super(title, is_img);
        this.text = text;
        this.arrayTask = arrayTask;
    }
}